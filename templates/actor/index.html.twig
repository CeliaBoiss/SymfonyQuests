{% extends 'base.html.twig' %}

{% block title %}Acteurs{% endblock %}

{% block body %}
<h2>Acteurs</h2>
<ul>
    {% for actor in actors %}
    <li><a href="{{ path('actor_show', {id : actor.id }) }}">{{ actor.name }}</a></li>
    {% else %}
    <li>Aucun acteur à afficher.</li>
    {% endfor %}
</ul>
{% endblock %}
