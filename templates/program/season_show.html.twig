{% extends 'base.html.twig' %}

{% block title %}Saison {{ season.number}} - {{ program.title }}{% endblock %}

{% block body %}
<h2>Saison {{ season.number }} de {{ program.title }}</h2>
<p>{{ season.description }}</p>
<p>Diffusion : {{ season.year }}</p>
<p>Liste des épisodes :</p>
<ul>
{% for episode in season.episodes %}
    <li>
        <a href="{{ path("program_episode_show", { programSlug: program.slug, seasonId: season.id, episodeSlug: episode.slug }) }}">Episode {{ episode.number }}</a>
    </li>
{% endfor %}
</ul>
<a href="{{ path('program_by_id', { slug: program.slug }) }}">Retour</a>
{% endblock %}